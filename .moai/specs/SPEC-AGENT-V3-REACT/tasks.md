# SPEC-AGENT-V3-REACT — Task Decomposition

> Phase 1.5 산출물. development_mode = **DDD** (ANALYZE-PRESERVE-IMPROVE).
> 정렬 원칙: (1) env infra 선행 → (2) **byte-identical 회귀 가드 + 기존 코드 characterization 먼저** → (3) sub-flag 별 갭 독립 구현 → (4) Reflexion deadline atomic 분리 → (5) 횡단 검증.
> 각 task 는 1 DDD 사이클 (ANALYZE 기존 코드 → PRESERVE characterization → IMPROVE flag-gated 증분) 로 완료 가능한 atomic 단위.

## Task Table

| Task ID | Description | Requirement | Dependencies | Planned Files | Status |
|---|---|---|---|---|---|
| T1 | [done] 5 env 추가 (`AGENT_V3_MEMORY_INJECTION_ENABLED`/`AGENT_V3_REFLEXION_ENABLED`/`AGENT_V3_PROACTIVE_ENABLED`/`AGENT_V3_DISLIKE_MEMORY_ENABLED` bool=False, `AGENT_V3_MEMORY_MAX_TOKENS` int=1500) — `AGENT_V2_*` 블록(config.py L195-205) 바로 아래 동일 패턴. `.env.example` 갱신. 회귀: 미설정 시 전부 false. | REQ-AGENT-V3-MEM-CAP-001 (env), 전 sub-flag FLAG REQ 의 전제 | — | `app/core/config.py`, `.env.example`, `tests/test_agent_v3/test_config_v3.py` | done |
| T2 | **byte-identical 회귀 가드 + 기존 코드 characterization (DDD PRESERVE 선행 게이트).** 현 V2 baseline 스냅샷 캡처: `run_react_loop` 초기 messages (`_SYSTEM_PROMPT`+`_build_user_message`), `TOOL_NAMES`/`REGISTRY` 7-tool 집합, search/refine ToolMessage content 직렬화 형식, `TasteProfile` 필드집합. 4 all-off 에서 AC-X.1 6 시나리오 + `tests/test_agent_v2/` 전 스위트 무변경 통과를 **갭 코드 0줄 상태에서** 먼저 확정 → P0 invariant 가 첫 갭 전에 testable. | REQ-AGENT-V3-COMPAT-ALLOFF-001 | T1 | `tests/test_agent_v3/test_byte_identical.py`, `tests/test_agent_v3/_v2_baseline.py` (스냅샷 헬퍼) | done |
| T3 | **Gap1 메모리 자동 주입.** NEW `_memory_context.py::build_memory_context(state,sess,ctx,*,max_tokens)->str` — `get_taste_store().get_or_create` + `boost_brands(5)`/`boost_keywords(5)`/`exclude_brands()` + `get_recent_history.dispatch({"n":5},ctx)` 래핑, char-cap(`max_tokens*4`) 최신 우선, `[MEMORY CONTEXT — SYSTEM DERIVED]` 펜스. MODIFY `react_loop.py` L305-308 messages 조립만 flag 분기 (`_build_user_message` 펜스 무변경). | REQ-AGENT-V3-MEM-INJECT-001, -MEM-CAP-001, -MEM-FLAG-001 | T2 | `app/agents/_memory_context.py` (NEW), `app/agents/react_loop.py`, `tests/test_agent_v3/test_gap1_memory.py` | done |
| T4 | **Gap1 보안 격리 검증.** 과거 turn user_text 5000자/주입 공격 → `_summarize_payload`(200자) cap + `[MEMORY CONTEXT — SYSTEM DERIVED]` 펜스 + `_build_user_message` 의 `[USER INPUT — DATA ONLY]` 펜스 V2 무변경 (이중 격리), 메모리 블록 char ≤ `AGENT_V3_MEMORY_MAX_TOKENS`*4. | REQ-AGENT-V3-SEC-001 | T3 | `tests/test_agent_v3/test_security_v3.py` | done |
| T5 | **Gap2 Reflexion 평가 래퍼 (V2 OQ-7 resolution).** NEW `_reflexion.py::evaluate_search_quality(state,sess,ctx)->dict` — `sess.last_results` 풀세트(OQ-V3-1) + `sess.user_intent` + vision_item 으로 `_evaluator_prompt.build_user_prompt`→`evaluator._call_llm` (빈 결과 `_build_fastpath_delta`). fail-open(score=1.0) 전파. **자체 LLM 프롬프트/평가 함수 신규 정의 금지** (AST 검증). | REQ-AGENT-V3-REFLEX-EVAL-001, -REFLEX-DELTA-001, REQ-AGENT-V3-COMPAT-WRAP-001 | T2 | `app/agents/_reflexion.py` (NEW), `tests/test_agent_v3/test_gap2_reflexion_eval.py` | done |
| T6 | **Gap2 in-loop 배선 + bound + flag-off.** MODIFY `react_loop.py` L556-596 dispatch-후/ToolMessage append 사이: flag ON & tool∈{search_products,refine_search} & result.ok → `_quality` 머지. per-turn ctx 카운터 `_v3_reflexion_count` < `SELF_CRITIQUE_MAX_ITERATIONS`(2) cap. evaluator 호출이 iteration counter 미증가 + `tool_call_history` append 무영향 (infinite-loop guard 보존). flag OFF → ToolMessage content V2 byte-identical (`json.dumps(result,default=str)[:2000]`), evaluator 0 호출. | REQ-AGENT-V3-REFLEX-BOUND-001, -REFLEX-FLAG-001, -REFLEX-DELTA-001 | T5 | `app/agents/react_loop.py`, `tests/test_agent_v3/test_gap2_reflexion_bound.py` | done |
| T7 | **Gap2 Reflexion 잔여-budget 강제 취소 (D2 P0, atomic).** `react_loop.py` 의 기존 `turn_deadline`(L299) 재사용해 `remaining=max(0.0,turn_deadline-time.monotonic())`. `remaining<=0`→evaluator skip. else `await asyncio.wait_for(evaluate_search_quality(...),timeout=remaining)` — overrun 시 `asyncio.TimeoutError` 강제 취소, `_quality={"skipped":True,"reason":"deadline"}`. pre-check 불충분 (`EVALUATOR_TIMEOUT_S`=8s > remaining) — 취소-on-overrun normative. | REQ-AGENT-V3-REFLEX-DEADLINE-001 | T6 | `app/agents/react_loop.py`, `tests/test_agent_v3/test_gap2_reflexion_deadline.py` | done |
| T8 | **Gap3 능동 제안.** NEW `tools/suggest_next_step.py::dispatch(args,ctx)->SuggestNextStepResult` — `_adapter_ctx.get_adapter()`+`send_text_with_buttons` 재사용 (ask_user_clarification 패턴, ≤80 LOC, 새 카드 렌더 금지). MODIFY `tool_registry.py`: `SuggestNextStepArgs/Result` TypedDict + 모듈 로드 시점(OQ-V3-6) flag 분기 8번째 entry. MODIFY `react_loop.py`: `_PROACTIVE_DIRECTIVE` 상수 + flag ON 시 system content append (조립 순서 `_SYSTEM_PROMPT [+_PROACTIVE_DIRECTIVE] [+memory_context]`, 약결과 `candidates_count<3` 선제 suggest / 모호 선제 clarify). flag OFF → `TOOL_NAMES` 길이 7 + `_SYSTEM_PROMPT` byte-identical. | REQ-AGENT-V3-PROACT-TOOL-001, -PROACT-PROMPT-001, -PROACT-FLAG-001 | T2 | `app/agents/tools/suggest_next_step.py` (NEW), `app/agents/tool_registry.py`, `app/agents/react_loop.py`, `tests/test_agent_v3/test_gap3_proactive.py` | done |
| T9 | **Gap4 크로스스레드 dislike (스키마 + DB 마이그레이션 + 디스카운트).** (a) MODIFY `taste_profile.py`: additive `disliked_brands_ts`/`disliked_keywords_ts: dict[str,float]=field(default_factory=dict)` (OQ-V3-3 단일 last-ts) + `recency_weighted_excludes(now,*,half_life_s)->tuple[list,list]` (OQ-V3-7 exponential, `exclude_brands` 재사용). Protocol/`update()`/`reinforce_*`/`_cap` 무변경. (b) **NEW Alembic 마이그레이션 `migrations/versions/0006_add_taste_dislike_ts.py`** — `ai.user_taste_profile` 에 2 JSONB 컬럼 ADD (default `{}`). (c) MODIFY `taste_profile_pg.py`: `_aupdate` INSERT/UPSERT(L113-141) +2 컬럼/`Jsonb()`/ON CONFLICT SET, `_aget_or_create` RETURNING(L95-97) +2, `_row_to_profile`(L184-204) +2 위치 backward-compat default. (d) MODIFY `update_taste.py`: flag ON 시 `reinforce_disliked_*` 직후 ts 기록. (e) MODIFY `search_products.py`/`refine_search.py`: flag ON 시 store dislike 읽어 기존 `exclude_keywords`/`AnalyzedItem` 경로 머지 (새 ranking 금지). 전 (a)-(e) flag OFF → V2 byte-identical. | REQ-AGENT-V3-DISLIKE-SCHEMA-001, -DISLIKE-DISCOUNT-001, -DISLIKE-FLAG-001 | T2 | `app/channels/taste_profile.py`, `app/channels/taste_profile_pg.py`, `migrations/versions/0006_add_taste_dislike_ts.py` (NEW), `app/agents/tools/update_taste.py`, `app/agents/tools/search_products.py`, `app/agents/tools/refine_search.py`, `tests/test_agent_v3/test_gap4_dislike.py`, `tests/test_memory_pg/test_migration_0006.py` (NEW) | done |
| T10 | **횡단 검증 — wrap-only + 성능 + 직교 edge.** `test_wrap_only.py` (AST: 헬퍼 import+호출, 신규 LLM/평가/ranking 함수 부재, `_reflexion` 가 `LLMProvider.chat` 직접 호출 없이 `evaluator._call_llm` 경유). `test_performance_v3.py` (4 flag ON 200턴 happy p95<8s / 50턴 exhausted p95<12s + slow-evaluator stub 잔여-budget 취소 정합 + `build_memory_context` 조립<50ms). edge E1-E6 (직교성/pairwise Gap2+Gap4·Gap1+Gap3/master OFF no-op/evaluator raise fail-open). 최종: `tests/test_agent_v2/` 전 스위트 4 all-off 무변경 통과 재확인 + pre-submission self-review. | REQ-AGENT-V3-COMPAT-WRAP-001, REQ-AGENT-V3-PERF-001, REQ-AGENT-V3-COMPAT-ALLOFF-001 | T3,T4,T6,T7,T8,T9 | `tests/test_agent_v3/test_wrap_only.py`, `tests/test_agent_v3/test_performance_v3.py`, `tests/test_agent_v3/test_edge_orthogonality.py` | done |

## DDD 사이클 메모 (per task)

- **기존 코드 touch (ANALYZE→PRESERVE→IMPROVE)**: T3/T6/T7 (`react_loop.py`), T8 (`react_loop.py`,`tool_registry.py`), T9 (`taste_profile.py`,`taste_profile_pg.py`,`update_taste.py`,`search_products.py`,`refine_search.py`), T1 (`config.py`). → 각 task 의 PRESERVE 단계에서 해당 파일 characterization 테스트 우선 (T2 가 react_loop/registry/taste 의 글로벌 baseline 을 선캡처하므로 task-local PRESERVE 는 그 위에 delta-scoped).
- **신규 파일 (full DDD = 신규지만 래핑 대상 기존 헬퍼 ANALYZE 필수)**: T3 (`_memory_context.py`), T5 (`_reflexion.py`), T8 (`suggest_next_step.py`), T9-(b) (Alembic 0006).
- **순수 신규 (테스트 only)**: T2/T4/T10 의 test 파일.

## Drift / Scope 경계

- planned files 외 수정 금지. 특히 `fashion_bot.py`(토폴로지)/`state.py`(WorkingState)/`evaluator.py` 본문/`conversation_log.py`/`taste_profile.py` 의 store Protocol·`update()`·`reinforce_*`·`exclude_brands`·`_cap` 는 **무변경 (snapshot/AST 검증)**.
- 누적 drift > 30% 시 Re-planning Gate (Phase 2.7).

## Open Question 처리 (Run 진입 전 plan.md 확정 완료, 재오픈 금지)

- OQ-V3-1 = `sess.last_results` 풀세트 (REQ-EVAL-001 본문 잠정 명시 → T5 채택 확정).
- OQ-V3-3 = 단일 last-ts (T9 채택). OQ-V3-6 = 모듈 로드 시점 flag 분기 (T8 채택). OQ-V3-7 = exponential recency (T9 채택).
- 잔여 run-blocking 항목은 progress.md / report 의 "SPEC ambiguity" 참조 (Gap4 DB 마이그레이션 배포 순서).
