# Progress: SPEC-AGENT-V2-REACT

- Plan phase complete: plan.md authored (15 tasks, all 10 OQs resolved)

## Wave 1 — Cross-SPEC + Foundation (complete)
- T-000 complete: ToolCallPayload TypedDict + 20th event_type "tool_call" added to event_payloads.py (conversation_log.py needs no change — generic emit)
- T-001 complete: WorkingState +3 fields (agent_iterations / tool_call_history[_LIST_ADD] / agent_status Literal)
- T-002 complete: app/agents/ package + tool_registry.py (7 args + 7 result TypedDicts + REGISTRY + ToolMetadata + validate_args)
- T-008 complete: 6 env vars in config.py + agent_v2_react_enabled/agent_llm_model_configured in /health/ready (fail-closed via AGENT_LLM_MODEL)

## Wave 2 — Tools (complete)
- T-003a complete: analyze_image.py — vision.extract wrapper + SSRF guard
- T-003b complete: search_products.py — run_pipeline wrapper
- T-003c complete: refine_search.py — critique re-search, no internal evaluator (OQ-7 α)
- T-003d complete: update_taste.py — TasteProfileStore wrapper, source enum validated
- T-003e complete: ask_user_clarification.py — inline keyboard card, clarify:{axis}:{value}
- T-003f complete: get_recent_history.py — conv_log SELECT + OQ-8 per-event whitelist, in_memory fail-soft
- T-003g complete: respond.py — adapter send_text + card pass-through, no _Flow enum

## Wave 3 — Core engine + graph integration (complete)
- T-004 complete: llm_client.py (bind_tools, fail-closed) + react_loop.py (iter cap, infinite-loop guard, token budget, per-LLM/tool timeout, JSON malform retry, args validation, tool_call emit, fallback respond)
- T-005 complete: nodes/agent.py — @observe span, react_loop wrapper, never propagates
- T-006 complete: fashion_bot.py V2 topology branch (flag+model gated), V1 byte-identical in else; _route_after_ingest_v2 / _route_after_pick_v2 / _route_after_vision_v2
- T-007 complete: ingest.py Step C — inline clarify:* boost_keywords (V2 flag), mid-onboarding → node_error

## Wave 4 — Deprecation markers + V2.1 stub (complete)
- T-014 complete: 5 modules (critique_apply/taste_update/respond/evaluator/router) DEPRECATED docstring + @MX:LEGACY; SPEC-AGENT-V2-CLEANUP-001 stub created (P3, draft)

## Wave 5 — Tests (partial — foundation coverage)
- T-009 partial: 4 of 9 test files created (test_state_extension, test_tool_registry, test_topology, test_agent_loop) — 26 tests green. Deferred: test_backward_compat, test_tool_call_logging, test_failure_modes, test_performance, test_security (see follow-ups)

## Wave 6 — Quality gate (complete for delivered scope)
- ruff check + ruff format: all green on new/changed files
- pytest tests/test_agent_v2/: 26 passed
- Full suite flag=false: 606 passed / 10 failed (all 10 pre-existing — verified via git stash on unmodified HEAD; 7 = test_critique_loop+routing env-broken, 3 = expected count-bump tests now fixed)
- Forbidden-path scope check: PASS (no onboard_/pipeline/providers/webhooks/recommend/clarify.py touched)

## Wave 7 — Post-review P0/P1 fixes (code + security review)
- P0-1 fixed: react_loop.py invalid-args history entry now includes `args_full` so the 3-consecutive-identical-call infinite-loop guard fires for repeated invalid-arg calls; added args_full strip to the infinite_loop_guard early-return path
- P1-2 fixed: refine_search.py removed `missing_image_url_in_ctx` early return — text-only turns now proceed via empty-image_url dense-path pipeline call (mirrors search_products.py); image_url-present behavior unchanged
- P1-3 fixed: agent.py turn_no replaced placeholder `10` with `state.turn_no or (state.turn_no or 0) + 1` (matches react_loop.py emit convention)
- P1-4 fixed: analyze_image.py `_ssrf_ok` adds UNCONDITIONAL hard-deny (non-http(s) schemes / 127.0.0.0-8 / ::1 / localhost / 10. / 172.16-31. / 192.168. / 169.254.) BEFORE allowlist; allowlist retained as narrowing layer (REQ-AGENT-SEC-URL-001)
- P1-5 fixed: tool_registry.py validate_args adds isinstance enforcement for top_k/n/min_price/max_price (int, bool rejected) + brand_likes/brand_dislikes/keyword_likes/keyword_dislikes/event_types/options (list); type mismatch returns same failure shape → react_loop records as invalid_args
- Tests: tests/test_agent_v2/test_security.py added (27 parametrized cases: SSRF hard-deny + positive control + validate_args type checks)
- ruff check app/ + ruff format: all green; pytest tests/test_agent_v2/: 53 passed (was 26; +27 new)
- Scope: only react_loop.py / refine_search.py / agent.py / analyze_image.py / tool_registry.py + new test file touched; skipped items (JSON-malform streak, all P2) left as-is per instruction
- T-010 complete: V1 regression migrated — 5n flag-aware / 4n property / 17n skipif / 6n deleted; full suite green both flags

## Wave 8 — Bedrock (nova-lite) compatibility fix (2026-05-15)
- Runtime verification: langchain-openai 0.3.34 `bind_tools()` does NOT inject `tool_choice` when arg omitted (source-confirmed + real `.ainvoke()` against `nova-lite` returns valid `tool_calls`, `finish_reason=tool_calls`, no HTTP 400). gpt-4o-mini blocked only by OpenAI account quota (429) — request shape valid, unrelated to tool_choice.
- llm_client.py fix: explicit `bind_tools(..., tool_choice=None)` (lock the no-inject contract against future langchain bumps) + `ChatOpenAI(extra_body={"drop_params": True})` (LiteLLM strips provider-unsupported params for Bedrock instead of 400). Both no-op for OpenAI → AGENT_LLM_MODEL stays swappable. Minimal diff, lazy-singleton + fail-closed preserved.
- react_loop.py review: no Bedrock-incompatible assumptions broken (tool_call id pass-through, finish_reason not inspected, content-vs-tool_calls via langchain-normalized `.tool_calls`, JSON-malform retry provider-agnostic). 1 latent non-live note: `tc.get("id", it)` fallback would mismatch a Bedrock tool-use id IF LiteLLM omits the id — Nova always returns one (verified); not fixed (minimal-diff).
- plan.md: OQ-1 false Bedrock premise corrected (`gpt-4o-mini`→`nova-lite` default; Bedrock IS configured infra), OQ-2 Bedrock `tool_choice` caveat + fix documented, §1 summary table rows + HISTORY line added.
- Tests: tests/test_agent_v2/test_llm_client.py added (2 mock-level cases: no forced tool_choice + drop_params + fail-closed). pytest tests/test_agent_v2/: 55 passed. ruff check + format: green.

- agent_v2 hotfix: assistant tool_use turn now appended before tool_result (Bedrock Nova multi-turn 400 fix — Expected toolResult blocks)

## Wave 9 — Topology-integration redesign (2026-05-15, plan-only)
- Runtime (real Telegram, AGENT_V2_REACT_ENABLED=true, nova-lite) surfaced 4 defects: (1) ingest still calls V1 route_text every text turn → TimeoutError fallback + 3s waste + sess.state pollution; (2) contentless empty Update falls through _route_after_ingest_v2 → agent hallucination; (3) remember_lang flips sticky to en on Pinterest URL-only input; (4) V1 awaiting_intent state-machine still driving V2 routing.
- Pick-callback investigation VERDICT: the "empty webhook" is NOT a dropped pick tap. Evidence: parse_inbound builds callback_data="item:N" (adapter.py:308), _route_after_ingest_v2 routes item:→pick_item (fashion_bot.py:221), pick_item sets selected_item_index (pick_item.py:148), _route_after_pick_v2 selected_item_index→agent (fashion_bot.py:247). pick→tap→agent chain correctly wired in V2; empty webhook is a separate genuinely-contentless Telegram update. Pick path needs NO change (regression-fix test only).
- plan.md: §Task 6 + §Task 7 rewritten (route_text V2-skip gate + empty-input END guard), §Task 7.5 (lang URL-only guard) + §Task 15 (topology-integration redesign: 5 decisions w/ pseudocode-level gates + file-by-file change list + per-fix AC) added. HISTORY +1 line, task count 15→17. Decision 4 Session.state consumer audit: route_text/_route_after_ingest(V1)=V1-only safe; AWAITING_ITEM_PICK+RESULTS_SENT=shared (latter = known V2 limitation, RESULTS_SENT re-query absent, follow-up not crash). No OQ invalidated; OQ-4 now completed by Decision 1/4.
- Scope: plan.md only. No app/ or tests/ modified.

## Wave 10 — Topology-integration redesign IMPLEMENTATION (2026-05-15)
- Change 1 (§15 Decision 1, ingest.py): added module-level `from app.core.config import settings`; inserted V2 early-return guard immediately before the `needs_router` block (after Step C). Gate: `settings.AGENT_V2_REACT_ENABLED and (settings.AGENT_LLM_MODEL or "").strip()` (byte-identical to fashion_bot.py build_graph gate). V2 path: `_emit_intent_routed(state, None)` then `return {"log_events": breadcrumbs, "turn_no": 1}` — same shape as the `not needs_router` path, route_text never invoked, decision unset. V1 needs_router/route_text/except block 100% untouched (verified via git diff — purely additive).
- Change 2 (§15 Decision 2, fashion_bot.py): added empty-input guard in `_route_after_ingest_v2` after all positive branches, before `return "agent"`: `not (msg.text or "").strip() and not msg.callback_data and not msg.urls and not msg.photo_file_id → return "__end__"`. Added `"__end__": END` to `ingest_branches_v2`. Silent (no adapter send). _route_after_vision_v2/_route_after_pick_v2 untouched.
- Change 3 (§15 Decision 5, lang.py): added module-level `_URL_TOKEN_RE`; URL-only guard in `remember_lang` after the <3-char guard: all whitespace-split tokens URL-like → `return prior`. detect_lang/session_lang untouched.
- Change 4 (Decision 3, pick_item): NO code change — characterization test only.
- state.decision dead-in-V2 PROOF: grep — sole consumer is routing.py `_route_after_router_text` (lines 276-286), bound only at fashion_bot.py:325 inside the V1 builder (after the line-289 `return _build_graph_v2()` early-return). _build_graph_v2's closures (`_route_after_ingest_v2`/`_route_after_pick_v2`/`_route_after_vision_v2`), agent.py, app/agents/* reference 0. Confirmed dead. No contradiction.
- Tests added: tests/test_agent_v2/test_ingest_route_text_gate.py (3), test_ingest_v2_routing.py (5 incl. pick happy-path characterization), test_lang_url_guard.py (14 parametrized + 2). tests/test_agent_v2: 78 passed (was 56, +22).
- ruff check . + ruff format --check .: All checks passed, 215 files already formatted (fashion_bot.py reformatted: multi-line if collapsed to 1 line ≤120).
- KNOWN REGRESSION (out-of-scope, reported): tests/test_graph_nodes/test_node_error_handling.py::test_ingest_router_exception_does_not_propagate fails under .env V2-active env. It is a V1 router-path characterization test that calls ingest() directly WITHOUT pinning settings to V1; under .env (AGENT_V2_REACT_ENABLED=true, AGENT_LLM_MODEL=nova-lite) the §15 Decision 1 guard correctly skips route_text so the asserted `ingest_error` breadcrumb never appears. V1 path proven byte-identical (settings-pinned repro: route_text called → raises → ingest_error breadcrumb present). Fix requires editing test_node_error_handling.py (outside allowed touch scope; plan Task T-010 territory). Clean baseline (no changes, no new tests): 643 passed/0 failed → with changes: 664 passed/1 failed.
- ROOT BUG FIX (pick-item hallucination): `react_loop.py::_build_user_message` under-populated LLM context — on `item:N` callback it emitted only `lang_hint`+`callback: item:0`, never the selected item or vision details, so nova-lite hallucinated "최근 대화 내용을 불러오는 데 문제가 있네요". Fix (react_loop.py only, additive): new `_detected_items`/`_item_attrs`/`_attr_tail` helpers; on `selected_item_index` in range → emit `user_selected_item: <label> (<subcat>/<fit>/<color>)` + `suggested_query: "<searchQueryKo|searchQuery>"` (ko-aware), replacing opaque `callback:` line; else vision-ran → `detected_items:` summary (+ `previously_picked_item` from sess.vision_item); out-of-range/empty handled gracefully (no IndexError). Vision/pick context placed OUTSIDE the `[USER INPUT — DATA ONLY]` fence (system-derived, trusted). `_SYSTEM_PROMPT` +1 sentence: when `user_selected_item:` present, call search_products with suggested_query then respond. `_build_ctx` unchanged (tool resolves image_url from ctx, query from LLM args — minimal touch).
- LIVE nova-lite proof (real LITELLM_BASE_URL, downstream pipeline I/O stubbed since fixture image URL can't embed): picked item:0 turn → tool_call_history=['search_products','respond'], agent_status=done (was: exhausted/hallucinated). Empty-input → ['respond'], done. Structural: _build_user_message output contains label + ko suggested_query.
- Tests: +2 regression (test_build_user_message_includes_picked_item, ..._handles_out_of_range_index). test_agent_v2 flagged: 80 passed (was 78). CI-equiv (no env) tests/test_agent_v2+test_graph_nodes+test_config: 149 passed, 1 skipped. ruff check + format --check: clean, 215 files.

## Wave 11 — First-touch service intro (onboarding-cards OFF) (2026-05-16)
- Policy change: mood/color/fit/pinterest onboarding card flow disabled (.env ONBOARDING_CARDS_ENABLED=false, set by orchestrator). Gap: with cards off, a brand-new user (onboarded_at IS NULL) fell straight into the V2 agent with no service introduction. Fix: on a new user's FIRST message, send ONE deterministic detailed friendly intro message, mark introduced, terminate (first message NOT processed by agent); 2nd message onward → normal V2 flow.
- New node `app/graphs/nodes/intro.py` (~115 LOC incl. KO/EN message constants, node body ≤40 LOC): `@observe(name="node.intro")`. Sends `_INTRO_KO`/`_INTRO_EN` via `factory.get_adapter().send_text` (same import as onboard_intro.py), KO/EN by `session_lang(sess)` (default KO via DEMO_MODE or no-Hangul→en→but audience-default handled by lang). Persists `sess.onboarded_at = datetime.now(UTC)` + `get_store().update(sess)` (mirrors `_onboard_helpers.complete_onboarding` step 4 exactly — survives next webhook: in-memory keeps same Session ref; PG session_pg.py:216 upserts `onboarded_at = EXCLUDED.onboarded_at`). Emits `bot_text` conv_log breadcrumb (flow="intro"). Terminal → END.
- New predicate `routing.first_touch_intro_required(state, sess)`: True iff `not ONBOARDING_CARDS_ENABLED` AND `sess.onboarded_at IS NULL` AND inbound carries user signal (`cb or (msg.text or "").strip() or photo or urls`) — blank-text contentless Updates fall through to the §15 Decision 2 silent-`__end__` guard, not intro. `onboarding_required` UNTOUCHED.
- Routing wire (`_route_after_ingest_v2`): added `first_touch_intro_required` check immediately AFTER `onboarding_required` (unchanged when flag ON) and `is_continuous_pinterest`, BEFORE the `🧭 [route_v2]` diagnostic log + item:/photo/url/pick/empty-guard branches → `return "intro"`. Diagnostic logs (`🧭 [route_v2]`, webhook `cb=%r`) NOT altered. Added `"intro": "intro"` to `ingest_branches_v2`, `builder.add_node("intro", intro_node)`, `builder.add_edge("intro", END)`. All edits inside `_build_graph_v2()` only — V1 `build_graph()` body byte-identical; onboarding-ON path byte-identical (flag ON → onboarding_required True → never reaches new predicate).
- Edge cases: first-msg-is-Pinterest-URL → intro-only (test asserts NOT resolve_image); `/start` first → intro (test); persistence-survives-next-webhook proven by test_second_message_skips_intro (2 sequential ainvoke, 2nd → agent, intro sent exactly once); contentless → silent END untouched (test, onboarded_at stays None).
- Onboarding node bodies / agent.py / react_loop.py / .env NOT modified (flag-off rollback mechanism preserved).
- Tests: tests/test_agent_v2/test_intro_first_touch.py (7 cases: new-user KO route+content+persist, EN, 2nd-msg→agent, Pinterest-URL intro-only, /start intro, contentless silent-END, flag-ON regression guard uses onboard_intro not intro). AGENT_V2_REACT_ENABLED=true AGENT_LLM_MODEL=nova-lite tests/test_agent_v2: 87 passed (was 80, +7). CI-equiv (env -u all three) tests/test_agent_v2+test_graph_nodes+test_config: 149 passed, 1 skipped, 7 failed — ALL 7 pre-existing baseline failures (verified via `git stash` repro: identical 7 failed/32 passed with changes removed; caused by .env ONBOARDING_CARDS_ENABLED=false loaded by pydantic-settings regardless of shell -u, NOT by this change). ruff check . + format --check .: All checks passed, 217 files formatted.
- Runtime hardening (2026-05-16): Fix A — appended "Avoid redundant tool calls" block to `react_loop._SYSTEM_PROMPT` (additive; persona/lang/fence rules byte-preserved): (1) prior search/refine candidates → next action MUST be `respond`, no re-search, no `analyze_image`; (2) `analyze_image` forbidden when `detected_items:`/`user_selected_item:`/`style_node:` already in user msg; (3) fewest calls, no identical-arg repeats. Fix B — transient-error retry: new `_is_transient(exc)` (5xx{500,502,503,504} via .status_code/.response.status_code, asyncio/builtin Timeout, throttle/5xx/timeout message markers; 4xx markers fail closed → NON-transient; conservative-unknown=False). LLM `ainvoke` wrapped in retry loop (max 2, backoff 0.6/1.4s) — a retry does NOT consume a ReAct iteration; tool dispatch wrapped (max 1, backoff 0.8s). Hard per-turn wall-clock ceiling `turn_deadline` bounds retries WITHIN existing exhaust envelope; token-budget/iteration-cap/infinite-loop/respond-terminal semantics untouched. New config knobs `AGENT_LLM_MAX_RETRIES=2`, `AGENT_TOOL_MAX_RETRIES=1`. Each retry logged WARNING with attempt#+reason. Diagnostic logs (`🧭 [route_v2]`, webhook `cb=%r`), llm_client.py, tool_registry.py, fashion_bot.py topology, ingest/intro/lang/.env NOT touched. Live nova-lite verify: picked-item turn now `tool_call_history=['search_products','respond']` — redundant analyze_image-after-search ELIMINATED; transient sim 1x500→recovered(done,2 calls), 3x500→graceful fallback(exhausted,3 calls,1 msg), 400→no retry(1 call). Tests: +21 (`_is_transient` 17-row truth table, LLM-retry-then-success, LLM-retry-exhausted→fallback, tool-dispatch-transient-retry, non-transient-no-retry, system-prompt-contains-redundancy-rules). AGENT_V2_REACT_ENABLED=true tests/test_agent_v2: 108 passed (was 87, +21). CI-equiv (env -u all): 108 passed, no new failures. ruff check + format --check: clean (217 files).
