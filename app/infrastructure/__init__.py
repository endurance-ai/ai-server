"""Infrastructure layer (SPEC-ARCH-AI-001).

Repositories (PR2) and memory persistence (PR4). Memory modules were
relocated here from app/channels/ with thin re-export shims left at the old
paths (REQ-AI-004); behavior is byte-identical (pure move).
"""
